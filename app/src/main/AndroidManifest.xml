<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tomoko.testapplication">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".Client"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ConnectActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
           android:label="@string/app_name">
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".RegistUserActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".NoticeActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".NoticeMoreActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".MoneyActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".TalkManageActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".GroupActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".AddGroupActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".QuestionRoomActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".WatchQuestionActivity"
            android:label="@string/app_name" />
        <activity android:name=".CheckQuestionActivity" />
        <activity android:name=".AskQuestionActivity" />
        <activity android:name=".AnswerAppActivity" />
        <activity android:name=".AnswerCheckActivity" />
        <activity android:name=".OfferCheckActivity" />
        <activity android:name=".ApplicantActivity" />
        <activity android:name=".AnswerListActivity" />
        <activity android:name=".AnswerEquivalentActivity" />
        <activity android:name=".ReturnAnswerActivity" />
        <activity android:name=".GroupSearchActivity" />
        <activity android:name=".GroupMemberActivity" />

        <!-- <activity android:name=".GroupSearchActivity"></activity> -->
        <activity android:name=".RanCheckActivity" />

    </application>

</manifest>